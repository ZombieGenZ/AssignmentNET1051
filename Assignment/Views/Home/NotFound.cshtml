@{
    Layout = null;
    ViewData["Title"] = "Không tìm thấy trang";
}
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - Assignment</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        :root {
            --primary: #ff6b35;
            --secondary: #0f172a;
            --accent: #facc15;
            --background: #0b1120;
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: 'Be Vietnam Pro', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: radial-gradient(circle at 20% 20%, rgba(255, 107, 53, 0.35), transparent 55%),
                        radial-gradient(circle at 80% 0%, rgba(250, 204, 21, 0.3), transparent 60%),
                        radial-gradient(circle at 10% 80%, rgba(96, 165, 250, 0.25), transparent 55%),
                        var(--background);
            color: #e2e8f0;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem 1.5rem;
        }

        .notfound-wrapper {
            max-width: 900px;
            width: 100%;
            background: rgba(15, 23, 42, 0.75);
            border-radius: 24px;
            box-shadow: 0 30px 80px rgba(15, 23, 42, 0.45);
            backdrop-filter: blur(10px);
            overflow: hidden;
            position: relative;
        }

        .notfound-wrapper::before,
        .notfound-wrapper::after {
            content: '';
            position: absolute;
            border-radius: 50%;
            filter: blur(80px);
            z-index: 0;
        }

        .notfound-wrapper::before {
            width: 220px;
            height: 220px;
            background: rgba(255, 107, 53, 0.4);
            top: -70px;
            right: -60px;
        }

        .notfound-wrapper::after {
            width: 180px;
            height: 180px;
            background: rgba(59, 130, 246, 0.35);
            bottom: -60px;
            left: -50px;
        }

        .notfound-content {
            position: relative;
            z-index: 1;
            padding: 3.5rem 3rem 3rem;
            text-align: center;
        }

        .status-code {
            font-size: clamp(6rem, 12vw, 9rem);
            font-weight: 700;
            letter-spacing: 0.1em;
            margin: 0;
            background: linear-gradient(120deg, #f97316, #facc15, #38bdf8);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .status-message {
            margin-top: 1rem;
            font-size: clamp(1.25rem, 2.5vw, 1.75rem);
            font-weight: 600;
            color: #f1f5f9;
        }

        .status-description {
            margin: 1.5rem auto 2.5rem;
            max-width: 580px;
            line-height: 1.7;
            color: #cbd5f5;
        }

        .cta-buttons {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 1rem;
            margin-bottom: 2.5rem;
        }

        .cta-buttons a {
            display: inline-flex;
            align-items: center;
            gap: 0.6rem;
            padding: 0.85rem 1.6rem;
            border-radius: 999px;
            text-decoration: none;
            font-weight: 600;
            transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
        }

        .cta-buttons a.primary {
            background: linear-gradient(135deg, var(--primary), #f59e0b);
            color: #0f172a;
            box-shadow: 0 12px 25px rgba(255, 107, 53, 0.35);
        }

        .cta-buttons a.primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 18px 28px rgba(255, 107, 53, 0.45);
        }

        .cta-buttons a.secondary {
            background: rgba(15, 23, 42, 0.8);
            color: #f1f5f9;
            border: 1px solid rgba(148, 163, 184, 0.35);
        }

        .cta-buttons a.secondary:hover {
            transform: translateY(-3px);
            background: rgba(15, 23, 42, 0.95);
        }

        .support-card {
            background: rgba(15, 23, 42, 0.65);
            border: 1px solid rgba(148, 163, 184, 0.2);
            border-radius: 18px;
            padding: 1.5rem 1.75rem;
            text-align: left;
            max-width: 560px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: auto 1fr;
            gap: 1rem 1.25rem;
            align-items: center;
        }

        .support-card i {
            font-size: 1.8rem;
            color: #facc15;
        }

        .support-card strong {
            display: block;
            font-size: 1.05rem;
            color: #f8fafc;
            margin-bottom: 0.25rem;
        }

        .support-card span {
            color: #cbd5f5;
            font-size: 0.95rem;
        }

        @media (max-width: 640px) {
            .notfound-content {
                padding: 2.75rem 1.75rem;
            }

            .cta-buttons {
                flex-direction: column;
            }

            .support-card {
                grid-template-columns: 1fr;
                text-align: center;
            }

            .support-card i {
                justify-self: center;
            }
        }
    </style>
</head>
<body>
    <div class="notfound-wrapper">
        <div class="notfound-content">
            <p class="status-code">404</p>
            <p class="status-message">Có vẻ bạn đã lạc vào không gian rỗng!</p>
            <p class="status-description">
                Trang bạn đang tìm không tồn tại hoặc đã được di chuyển. Hãy quay lại trang chủ hoặc khám phá những ưu đãi mới nhất của chúng tôi.
            </p>
            <div class="cta-buttons">
                <a class="primary" href="@Url.Action("Index", "Home")">
                    <i class="fas fa-home"></i> Về trang chủ
                </a>
                <a class="secondary" href="@Url.Action("Index", "Cart")">
                    <i class="fas fa-gift"></i> Xem ưu đãi mới
                </a>
            </div>
            <div class="support-card">
                <i class="fas fa-life-ring"></i>
                <div>
                    <strong>Cần hỗ trợ ngay?</strong>
                    <span>Liên hệ với chúng tôi qua email <a href="mailto:support@assignment.local">support@assignment.local</a> hoặc hotline <strong>1900 999 888</strong>.</span>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
